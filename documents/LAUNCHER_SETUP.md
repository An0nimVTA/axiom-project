# AXIOM MULTIVERSE v2.0 - Инструкция по запуску

## 🚀 Быстрый запуск

### 1. Подготовка Backend API (обязательно первым!)

```bash
cd backend
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate   # Windows

pip install -r requirements.txt
python3 setup_db.py
python3 init_db.py  # Для тестовых данных
python3 app.py      # Оставить запущенным
```

### 2. Запуск лаунчера AXIOM MULTIVERSE v2.0

```bash
cd launcher
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate   # Windows

pip install -r requirements.txt
python3 main.py
```

---

## 🎨 Особенности интерфейса

- **Верхняя панель**: Логотип AXIOM, вкладки [ГЛАВНАЯ], [НОВОСТИ], настройки, язык
- **Левая панель**: Серверы (чёрные карточки 80px с закруглёнными углами)
- **Центральная панель**: Описание выбранного сервера
- **Правая панель**: Новости
- **Нижняя панель**: Кнопка "ИГРАТЬ"

---

## 🎯 Динамические темы

При выборе сервера тема меняется автоматически (анимация 300мс):

| Тип сервера | Цветовая схема |
|-------------|----------------|
| Современный | Синий (#3b82f6) + Серебро (#e0e0e0) + Белый (#ffffff) |
| Средневековый | Тёмно-зелёный (#166534) + Золото (#d97706) + Кожа (#d2b48c) |
| Средневековый (магический) | Фиолетовый (#a855f7) + Бронза (#cd7f32) + Чёрный (#000000) |
| Мини-игры | Оранжевый (#f97316) + Белый (#ffffff) + Серый (#94a3b8) |

---

## 🛡️ Аутентификация

- **Регистрация**: Логин 3-20 символов (только латиница, цифры, подчёркивание), пароль 8+ символов
- **Подтверждение пароля**: При регистрации
- **Вход**: По логину/паролю (без Discord OAuth)
- **Безопасность**: Пароли хешируются bcrypt, JWT токены

---

## 🌐 Локализация

- **Поддерживаемые языки**: Русский (ru), Английский (en)
- **Переключение**: Без перезагрузки приложения
- **Формат файлов**: JSON в assets/lang/

---

## 🔄 Автообновление

- **Интервал проверки**: Каждые 3-5 минут
- **Проверка версии**: GET запрос к https://api.axiom.dev/launcher/version
- **Скачивание**: В фоновом режиме
- **Уведомление**: При наличии новой версии

---

## ⚙️ Настройки

Доступны через кнопку ⚙️ (шестерёнка):

- **Профиль**:
  - Выход из аккаунта
  - Смена аккаунта

- **Приложение**:
  - Автообновление сервера
  - Переключение языка (RU/EN)

---

## 📁 Структура хранения данных

- **Windows**: `%APPDATA%/AXIOM/`
- **Linux/Mac**: `~/.axiom/`

```
%APPDATA%/AXIOM/
├── logs/               # Логи (launcher.log)
├── cache/              # Кэш и обновления
├── assets/             # Ресурсы (изображения, иконки, фон)
├── instances/          # Экземпляры серверов с модпаками
├── config.json         # Конфигурация пользователя
└── launcher.db         # Локальная БД (опционально)
```

---

## 📥 Запуск сервера с модпаками

1. **Проверка модпака**: Если нет - скачивание с CDN
2. **Установка**: Распаковка в экземпляр сервера
3. **Запуск Minecraft**: С подключением к скрытому серверу через прокси
4. **Аргументы запуска**: 
   - `--server {server_endpoint}`
   - `--port {server_port}`
   - `--username {login}`
   - `--token {jwt_token}`

---

## 🐛 Решение проблем

### Лаунчер не запускается
- Проверьте зависимости: `pip install -r requirements.txt`
- Убедитесь, что Python 3.10+ установлен
- В Windows: установите `python3-tk`: `sudo apt-get install python3-tk`

### Серверы не отображаются
- Убедитесь, что Backend API запущен
- Проверьте подключение к БД: `python3 check_db.py`
- Загрузите тестовые данные: `python3 init_db.py`

### Ошибка аутентификации
- Backend API должен быть запущен
- Убедитесь, что таблицы в БД созданы
- Проверьте логи Backend API

### Нет динамических тем
- Проверьте, что server.type правильно передаётся
- Убедитесь, что theme_manager.py обновлён

---

## 🎯 Критерии приёмки

Лаунчер считается полностью рабочим, если:

- ✅ Запускается и показывает форму входа
- ✅ Регистрация и вход работают через БД
- ✅ IP-адреса серверов никогда не видны
- ✅ Тема интерфейса меняется при выборе сервера
- ✅ Есть меню настроек с выходом, сменой языка, автообновлением
- ✅ Лаунчер проверяет обновления каждые 3-5 минут
- ✅ Поддерживается RU/EN
- ✅ Весь интерфейс соответствует стилю Cristalix + брендбуку AXIOM
- ✅ Запуск сервера с модпаками работает корректно
- ✅ Все элементы интерфейса соответствуют ТЗ