# AXIOM Infrastructure Setup

## 🚀 Автоматизация

### 1. CI/CD (GitHub Actions)
**Файл:** `.github/workflows/ci.yml`

**Что делает:**
- Автоматическая сборка при push в main/develop
- Сборка всех 3 компонентов (Plugin, Mod, Launcher)
- Создание релизов с JAR файлами
- Запуск тестов

**Активация:**
```bash
cd "/home/an0nimvta/axiom plugin"
git add .github/workflows/ci.yml
git commit -m "Add CI/CD pipeline"
git push
```

---

### 2. Auto-Build Script
**Файл:** `build.sh`

**Что делает:**
- Собирает все 3 компонента одной командой
- Создает директорию с датой и временем
- Копирует готовые JAR файлы

**Использование:**
```bash
cd "/home/an0nimvta/axiom plugin"
./build.sh
```

**Результат:**
```
builds/
└── 20260124_160000/
    ├── axiom-plugin.jar
    ├── axiomui-mod.jar
    └── axiom-launcher.jar
```

---

### 3. Backup System
**Файл:** `backup.sh`

**Что делает:**
- Backup кода проекта
- Backup сервера (без world)
- Backup базы данных
- Удаляет старые backup (>7 дней)

**Использование:**
```bash
cd "/home/an0nimvta/axiom plugin"
./backup.sh
```

**Автоматизация (cron):**
```bash
# Backup каждый день в 3:00
0 3 * * * /home/an0nimvta/axiom\ plugin/backup.sh
```

---

### 4. Monitoring System
**Файл:** `monitor.sh`

**Что делает:**
- Проверяет статус сервера
- Мониторит использование памяти
- Считает игроков
- Проверяет ошибки в логах
- Проверяет использование диска

**Использование:**
```bash
cd "/home/an0nimvta/axiom plugin"
./monitor.sh
```

**Автоматизация (cron):**
```bash
# Мониторинг каждые 5 минут
*/5 * * * * /home/an0nimvta/axiom\ plugin/monitor.sh
```

---

## 📊 Настройка автоматизации

### Шаг 1: Активировать CI/CD
```bash
cd "/home/an0nimvta/axiom plugin"
git add .github/workflows/ci.yml
git commit -m "Add CI/CD pipeline"
git push
```

### Шаг 2: Настроить cron для backup
```bash
crontab -e
```

Добавить:
```
# AXIOM Backup (каждый день в 3:00)
0 3 * * * /home/an0nimvta/axiom\ plugin/backup.sh

# AXIOM Monitor (каждые 5 минут)
*/5 * * * * /home/an0nimvta/axiom\ plugin/monitor.sh
```

### Шаг 3: Проверить работу
```bash
# Тест сборки
./build.sh

# Тест backup
./backup.sh

# Тест мониторинга
./monitor.sh
```

---

## 🔧 Дополнительные улучшения

### 1. Автоматический перезапуск сервера
**Файл:** `server/restart.sh`
```bash
#!/bin/bash
pkill -f "mohist.*server.jar"
sleep 5
cd /home/an0nimvta/axiom\ plugin/server
./start.sh
```

### 2. Уведомления в Discord/Telegram
Добавить в `monitor.sh`:
```bash
# При ошибках отправлять уведомление
if [ $errors -gt 10 ]; then
  curl -X POST "https://discord.com/api/webhooks/..." \
    -d '{"content":"⚠️ AXIOM: Много ошибок на сервере!"}'
fi
```

### 3. Автоматическое обновление
**Файл:** `update.sh`
```bash
#!/bin/bash
cd /home/an0nimvta/axiom\ plugin
git pull
./build.sh
./backup.sh
# Копировать новые JAR на сервер
```

---

## ✅ Готово!

Инфраструктура настроена:
- ✅ CI/CD для автосборки
- ✅ Скрипт быстрой сборки
- ✅ Система backup
- ✅ Мониторинг сервера

**Следующий шаг:** Активировать автоматизацию (cron + GitHub Actions)
