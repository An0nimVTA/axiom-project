# 📜 Спецификация и Условности Проекта AXIOM

Этот документ описывает **фундаментальные правила** и **технические ограничения** проекта. Все разработки должны строго им соответствовать.

---

## 1. ⚙️ Технические Условности

### Архитектура
*   **Ядро:** Гибридное (Mohist 1.20.1) = Forge Mods + Spigot/Paper Plugins.
*   **Java:** Строго **Java 17+**.
*   **Клиент:** Portable (переносной). Игрок не устанавливает Java и не настраивает TLauncher. Он запускает готовый `exe`.

### Лаунчер (AxiomLauncher)
*   **Принцип "Черного Ящика":** Лаунчер управляет ядром игры (`libraries`, `versions`, `config`, `assets`).
*   **Принцип "Свободы Модов":** Лаунчер **никогда** не удаляет и не перезаписывает папку `mods` (игрок может добавлять свои моды, например, на миникарту или оптимизацию, если они не конфликтуют).
*   **Авто-Вход:** Лаунчер обязан запускать игру с аргументом подключения к серверу, минуя главное меню.
*   **Изоляция:** Все файлы (Java, настройки, игра) лежат в одной папке с `exe`.

---

## 2. ⚖️ Геймплей и Баланс ("Правило Цепи")

Главное правило сборки: **Ни один крупный мод не должен быть самодостаточным.**

### Круговая Зависимость (Circular Dependency)
Игрок не может развиться в *Immersive Engineering*, не используя *Industrial Upgrade*, и наоборот.

*   **Оружие (TACZ):** Крафт требует детали из *Point Blank* + сталь из *Immersive Engineering*.
*   **Патроны:** Гильзы делаются в прессах *IE*, порох — химией из *Mekanism/Alchemistry*.
*   **Энергия:** Генераторы из одного мода требуют компоненты из другого.

**Цель:** Создать экономическую необходимость в торговле между игроками, специализирующимися на разных модах.

---

## 3. 🗺️ Политика и Нации (Серверная логика)

Вся логика управления миром лежит на **Java-плагинах** (Spigot API), а не на модах.

*   **Нации:** Это не просто кланы. Это структуры с территорией, налогами и иерархией.
*   **Города:** Безопасные зоны внутри нации.
*   **Война:**
    *   PVP разрешено везде, но разрушение блоков — только во время объявленной войны или осады.
    *   Использование огнестрела (TACZ) — основная боевая механика.

---

## 4. 🖥️ Интерфейс (UI)

*   **No Commands:** Игрок должен использовать команды чата (`/n create`) как можно реже.
*   **GUI First:** Всё управление должно идти через графические меню (написанные в `axiom-mod-integration`).
    *   Нажал `M` -> Открылось меню Нации.
    *   Нажал `K` -> Открылось дерево Технологий.
*   **Синхронизация:** Клиентский GUI просто отображает данные. Вся логика и базы данных находятся на сервере. Клиент запрашивает данные пакетами.

---

## 5. 📦 Файловая Структура (Стандарт)

При сборке релиза строго соблюдать структуру:

```text
AxiomClient/
├── AxiomLauncher.exe      # Точка входа
├── runtime/               # Встроенная Java (JRE 17)
├── lib/                   # Библиотеки лаунчера
└── minecraft/             # Рабочая папка игры
    ├── mods/              # Моды (не обновляются лаунчером)
    ├── config/            # Конфиги (обновляются)
    ├── versions/          # Forge и JSON (обновляются)
    ├── libraries/         # Библиотеки Minecraft (обновляются)
    └── assets/            # Звуки и текстуры (обновляются)

---

## 6. 🧪 Тестирование и QA (Контроль Качества)

В проекте действует принцип: **"Если это не протестировано — это не работает".**
Создание автоматических тестов (Auto-Tests) **строго поощряется** и имеет высокий приоритет при разработке.

### Что необходимо покрывать тестами:

1.  **🛠️ Крафты и Баланс (Высший приоритет):**
    *   **Recipe Integrity:** Авто-проверка того, что все измененные рецепты загрузились в `RecipeManager`.
    *   **Chain Validation:** Проверка "цепочки производства" — убедиться, что сложный предмет (например, `tacz:ak47`) технически возможно скрафтить из добываемых ресурсов, и нет "битых" ингредиентов.
    *   **Conflict Check:** Поиск дублирующихся рецептов у разных модов.

2.  **🎨 Интерфейс (UI) и Визуал:**
    *   **Widget Test:** Авто-открытие меню при старте тестов для проверки на краши.
    *   **Asset Validation:** Сканирование логов на наличие ошибок `Missing Texture` или `Missing Model`.
    *   **Scaling:** Проверка корректности отображения GUI на разных разрешениях экрана.

3.  **🔌 Серверная Логика:**
    *   Unit-тесты для экономики (транзакции, налоги).
    *   Тесты базы данных (сохранение/загрузка наций).

```
