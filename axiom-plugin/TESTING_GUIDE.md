# Руководство по тестированию AXIOM Geopolitical Engine

## Введение

Этот документ описывает систему автоматического тестирования проекта AXIOM Geopolitical Engine и объясняет, как использовать ее для обеспечения качества кода.

## Система автоматического тестирования

### AutoTestBot

**AutoTestBot** - это комплексная система автоматического тестирования, которая позволяет:
- Запускать все тесты одним командой
- Запускать конкретные тестовые сюиты
- Запускать отдельные тесты
- Получать детальные отчеты о результатах

### Структура тестов

```
Test Suites (6)
├── Service Tests (3 теста)
├── Repository Tests (2 теста)
├── Cache Tests (2 теста)
├── Validation Tests (2 теста)
├── Integration Tests (1 тест)
└── Performance Tests (1 тест)
```

### Доступные команды

#### 1. Запуск всех тестов
```
/test
```
Запускает все тесты и показывает общий отчет.

#### 2. Запуск конкретного сюита
```
/test suite <suiteName>
```
Примеры:
- `/test suite Service Tests`
- `/test suite Repository Tests`
- `/test suite Cache Tests`

#### 3. Запуск конкретного теста
```
/test test <testName>
```
Примеры:
- `/test test "MilitaryService Interface Test"`
- `/test test "NationRepository CRUD Test"`

#### 4. Просмотр доступных тестов
```
/test list
```
Показывает список всех доступных тестовых сюитов.

## Тестовые сюиты

### 1. Service Tests

Проверяет работу сервисов через интерфейсы.

**Тесты:**
- MilitaryService Interface Test
- SiegeService Interface Test
- EconomyService Interface Test

### 2. Repository Tests

Проверяет CRUD операции в репозиториях.

**Тесты:**
- NationRepository CRUD Test
- MilitaryRepository CRUD Test

### 3. Cache Tests

Проверяет работу системы кэширования.

**Тесты:**
- CacheManager Basic Test
- Cache TTL Test

### 4. Validation Tests

Проверяет работу валидации данных.

**Тесты:**
- Nation ID Validation Test
- Unit Type Validation Test

### 5. Integration Tests

Проверяет интеграцию между компонентами.

**Тесты:**
- Military Service Integration Test

### 6. Performance Tests

Проверяет производительность компонентов.

**Тесты:**
- Repository Performance Test

## Примеры использования

### Пример 1: Запуск всех тестов
```
/test
```

**Вывод:**
```
=== AUTO TEST BOT ===
Starting comprehensive testing...
Running suite: Service Tests
  ✓ MilitaryService Interface Test
  ✓ SiegeService Interface Test
  ✓ EconomyService Interface Test
Completed suite: Service Tests
Running suite: Repository Tests
  ✓ NationRepository CRUD Test
  ✓ MilitaryRepository CRUD Test
Completed suite: Repository Tests
... (другие сюиты)
=== TEST RESULTS ===
Total tests: 11
Passed: 11
Failed: 0
Duration: 450ms
All tests passed! 🎉
```

### Пример 2: Запуск конкретного теста
```
/test test "NationRepository CRUD Test"
```

**Вывод:**
```
Running test: NationRepository CRUD Test
  ✓ NationRepository CRUD Test
Test passed!
```

### Пример 3: Запуск тестового сюита
```
/test suite "Cache Tests"
```

**Вывод:**
```
Running suite: Cache Tests
  ✓ CacheManager Basic Test
  ✓ Cache TTL Test
Completed suite: Cache Tests
```

## Интеграция в процесс разработки

### 1. Локальное тестирование

Перед коммитом изменений:
```bash
# Запустить все тесты
/test

# Если есть конкретные изменения, запустить соответствующие тесты
/test suite "Service Tests"
```

### 2. Тестирование на сервере

После развертывания на тестовом сервере:
```
/test
```

### 3. Регулярное тестирование

Рекомендуется запускать все тесты:
- После значительных изменений
- Перед релизом новой версии
- Еженедельно для проверки стабильности

## Создание новых тестов

### Добавление нового тестового сюита

```java
private class NewFeatureTestSuite implements TestSuite {
    @Override
    public String getName() {
        return "New Feature Tests";
    }
    
    @Override
    public List<TestCase> getTestCases() {
        List<TestCase> tests = new ArrayList<>();
        
        tests.add(new TestCase() {
            @Override
            public String getName() {
                return "New Feature Basic Test";
            }
            
            @Override
            public boolean execute(CommandSender sender) {
                // Тестовая логика
                return true;
            }
        });
        
        return tests;
    }
}
```

### Добавление нового теста в существующий сюит

```java
// В соответствующем сюите
tests.add(new TestCase() {
    @Override
    public String getName() {
        return "New Test Name";
    }
    
    @Override
    public boolean execute(CommandSender sender) {
        // Тестовая логика
        return true;
    }
});
```

## Лучшие практики тестирования

### 1. Изоляция тестов
- Каждый тест должен быть независимым
- Не полагаться на состояние других тестов
- Очищать данные после теста

### 2. Покрытие кода
- Тестировать все основные сценарии
- Покрывать edge cases
- Проверять обработку ошибок

### 3. Производительность
- Тесты должны выполняться быстро
- Избегать тяжелых операций в тестах
- Использовать моки для внешних зависимостей

### 4. Читаемость
- Давать тестам понятные названия
- Документировать сложные тесты
- Следовать соглашениям о наименовании

## Отладка тестов

### 1. Логирование
```java
sender.sendMessage(ChatColor.YELLOW + "Debug: current value = " + value);
```

### 2. Пошаговое выполнение
- Запускать тесты по одному
- Проверять промежуточные результаты
- Использовать отладчик IDE

### 3. Проверка зависимостей
- Убедиться, что все зависимости инициализированы
- Проверять наличие тестовых данных
- Проверять права доступа

## Интеграция с CI/CD

Для интеграции с системами непрерывной интеграции:

```yaml
# Пример для GitHub Actions
name: AutoTestBot CI

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK
      uses: actions/setup-java@v2
      with:
        java-version: '17'
        distribution: 'adopt'
    - name: Run AutoTestBot
      run: ./gradlew test
```

## Заключение

Система автоматического тестирования **AutoTestBot** предоставляет мощный инструмент для обеспечения качества кода в проекте AXIOM Geopolitical Engine. Регулярное использование этой системы поможет:

- Предотвращать регрессии
- Улучшать качество кода
- Ускорять процесс разработки
- Повышать уверенность в стабильности проекта

Рекомендуется интегрировать запуск тестов в процесс разработки и использовать их перед каждым коммитом и релизом.

## Дополнительные ресурсы

- [JUnit 5 Documentation](https://junit.org/junit5/docs/current/user-guide/)
- [Mockito Framework](https://site.mockito.org/)
- [Test-Driven Development](https://en.wikipedia.org/wiki/Test-driven_development)
