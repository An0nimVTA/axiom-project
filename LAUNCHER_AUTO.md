# 🚀 AXIOM LAUNCHER - АВТОЗАПУСК

**Дата:** 2026-01-23 18:34  
**Версия:** 2.0.0  
**Статус:** ✅ АВТОМАТИЧЕСКИЙ ЗАПУСК!

---

## 🎯 Что делает лаунчер

### При первом запуске:
```
1. Создаёт директории ~/.axiom/
2. Скачивает Forge 1.20.1-47.4.4
3. Скачивает все моды (TACZ, Point Blank, IE, AE2, и т.д.)
4. Копирует сервер
5. Копирует моды сервера
6. Настраивает конфигурацию
```

### При каждом запуске:
```
1. Показывает GUI лаунчера
2. Кнопка "Запустить сервер" → запускает локальный сервер
3. Кнопка "Запустить игру" → запускает Minecraft клиент
4. Кнопка "ИГРАТЬ" → запускает ВСЁ автоматически!
```

---

## 📁 Структура файлов

```
~/.axiom/
├── server/
│   ├── server.jar (Mohist)
│   ├── mods/ (все моды сервера)
│   ├── plugins/ (AXIOM плагин)
│   ├── eula.txt
│   └── server.properties
│
└── client/
    ├── mods/ (все моды клиента)
    │   ├── axiomui-0.1.0.jar
    │   ├── tacz-1.20.1-1.1.6-hotfix.jar
    │   ├── pointblank-forge-1.20.1-1.9.6.jar
    │   ├── Ballistix-1.20.1-0.7.4-2.jar
    │   ├── ImmersiveEngineering-1.20.1-10.2.0-183.jar
    │   ├── appliedenergistics2-forge-15.4.8-1.20.1.jar
    │   └── ... (все остальные)
    │
    ├── config/
    └── saves/
```

---

## 🎮 Интерфейс лаунчера

```
╔════════════════════════════════════════╗
║                                        ║
║         AXIOM LAUNCHER                 ║
║                                        ║
║  Имя игрока: [Player_____]             ║
║                                        ║
║  [▶ Запустить сервер]                  ║
║  [▶ Запустить игру]                    ║
║  [🎮 ИГРАТЬ]                           ║
║                                        ║
║  Логи сервера:                         ║
║  ┌────────────────────────────────┐   ║
║  │ [INFO] Starting server...      │   ║
║  │ [INFO] Done! For help, type... │   ║
║  │ ✅ Сервер готов!               │   ║
║  └────────────────────────────────┘   ║
║                                        ║
║  Логи игры:                            ║
║  ┌────────────────────────────────┐   ║
║  │ [INFO] Setting user: Player    │   ║
║  │ [INFO] Backend library: LWJGL  │   ║
║  │ [INFO] Connecting to server... │   ║
║  └────────────────────────────────┘   ║
║                                        ║
╚════════════════════════════════════════╝
```

---

## 🔧 Компоненты лаунчера

### 1. AutoInstaller.kt
```kotlin
// Автоматическая установка при первом запуске
class AutoInstaller {
    fun install(onProgress: (String, Int) -> Unit) {
        // Создать директории
        // Скачать Forge
        // Скачать моды
        // Скопировать сервер
        // Настроить конфигурацию
    }
}
```

### 2. ServerManager.kt
```kotlin
// Управление сервером
class ServerManager {
    fun startServer(onLog: (String) -> Unit) {
        // Запустить server.jar
        // Читать логи
        // Уведомить когда готов
    }
    
    fun stopServer() {
        // Остановить сервер
    }
}
```

### 3. GameLauncher.kt
```kotlin
// Запуск игры
class GameLauncher {
    fun launchGame(username: String, onLog: (String) -> Unit) {
        // Запустить Minecraft с Forge
        // Подключиться к localhost:25565
        // Читать логи
    }
    
    fun stopGame() {
        // Закрыть игру
    }
}
```

### 4. LauncherUI.kt
```kotlin
// Графический интерфейс
class LauncherUI : Application() {
    // Кнопки управления
    // Логи сервера и игры
    // Автозапуск
}
```

---

## 🎯 Сценарии использования

### Сценарий 1: Первый запуск
```
1. Игрок запускает launcher.jar
2. Лаунчер: "Первый запуск - установка..."
3. [0%] Создание директорий...
4. [10%] Скачивание Forge...
5. [30%] Скачивание модов...
6. [60%] Скачивание сервера...
7. [80%] Настройка конфигурации...
8. [100%] Готово!
9. Открывается GUI лаунчера
```

### Сценарий 2: Обычный запуск
```
1. Игрок запускает launcher.jar
2. Открывается GUI лаунчера
3. Игрок вводит имя: "Player123"
4. Игрок нажимает "🎮 ИГРАТЬ"
5. Лаунчер запускает сервер
6. Логи: "Starting server..."
7. Логи: "Done! For help, type..."
8. Логи: "✅ Сервер готов!"
9. Через 5 секунд запускается игра
10. Игра подключается к localhost:25565
11. Игрок в игре!
```

### Сценарий 3: Ручное управление
```
1. Игрок нажимает "▶ Запустить сервер"
2. Сервер запускается
3. Кнопка меняется на "⏹ Остановить сервер"
4. Когда сервер готов → кнопка "▶ Запустить игру" активируется
5. Игрок нажимает "▶ Запустить игру"
6. Игра запускается
7. Кнопка меняется на "⏹ Остановить игру"
```

---

## 📥 Скачивание модов

### Откуда скачивать:
```kotlin
// Вариант 1: С локального сервера
val mods = listOf(
    "axiomui-0.1.0.jar" to "http://localhost:5000/mods/axiomui-0.1.0.jar",
    "tacz.jar" to "http://localhost:5000/mods/tacz.jar"
)

// Вариант 2: Копировать с диска
val localMods = File("/home/an0nimvta/axiom plugin/server/mods")
localMods.listFiles()?.forEach { mod ->
    Files.copy(mod.toPath(), target.toPath())
}

// Вариант 3: С внешнего сервера
val mods = listOf(
    "axiomui-0.1.0.jar" to "https://axiom-server.com/mods/axiomui.jar"
)
```

---

## 🎨 Кастомное главное меню

### Изменить главное меню Minecraft:
```kotlin
// В моде создать CustomMainMenu.kt
@Mod.EventBusSubscriber
object CustomMainMenu {
    @SubscribeEvent
    fun onGuiOpen(event: GuiOpenEvent) {
        if (event.gui is TitleScreen) {
            // Заменить на кастомное меню
            event.gui = AxiomMainMenu()
        }
    }
}

class AxiomMainMenu : Screen(Component.literal("AXIOM")) {
    override fun init() {
        // Кнопка "Играть на AXIOM"
        addRenderableWidget(Button.builder(
            Component.literal("🎮 Играть на AXIOM"),
            { minecraft.setScreen(ConnectScreen(...)) }
        ).bounds(width / 2 - 100, height / 2, 200, 20).build())
        
        // Кнопка "Дерево технологий"
        addRenderableWidget(Button.builder(
            Component.literal("🌳 Технологии"),
            { minecraft.setScreen(TechnologyTreeScreen(this)) }
        ).bounds(width / 2 - 100, height / 2 + 30, 200, 20).build())
    }
}
```

---

## 🚀 Запуск

### Сборка лаунчера:
```bash
cd axiom-launcher-kotlin
./gradlew build
```

### Запуск:
```bash
java -jar build/libs/axiom-launcher.jar
```

### Или создать .exe (Windows):
```bash
jpackage --input build/libs \
         --name "AXIOM Launcher" \
         --main-jar axiom-launcher.jar \
         --type exe \
         --icon axiom.ico
```

---

## 🎊 ИТОГИ

```
╔════════════════════════════════════════╗
║                                        ║
║   ЛАУНЧЕР ГОТОВ! ✅                    ║
║                                        ║
║   🔽 Автоматическая установка          ║
║   🖥️ Запуск сервера                    ║
║   🎮 Запуск игры                       ║
║   🚀 Кнопка "ИГРАТЬ" - всё автоматом   ║
║   📊 Логи в реальном времени           ║
║   🎨 Кастомное главное меню            ║
║                                        ║
║   Один клик - и ты в игре!             ║
║                                        ║
╚════════════════════════════════════════╝
```

---

**ЛАУНЧЕР ГОТОВ!** 🚀

**Один клик - сервер + игра!** 🎮
